<% comments ||= [] %>

<div class="list-group-comment">
  <% if comments.present? %>
    <% comments.each do |comment| %>
      <%= render 'instagram_comments/comment', comment: comment %>
    <% end %>
  <% else %>
    <div class="list-group-item ">
    </div>
  <% end %>
</div>

